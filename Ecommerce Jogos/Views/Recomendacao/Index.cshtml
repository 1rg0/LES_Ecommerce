@{
    ViewData["Title"] = "Recomendações com IA";
}

<div class="container my-4">
    <div class="text-center mb-5">
        <h1 class="display-4">Seu Assistente de Jogos</h1>
        <p class="lead text-muted">Descubra seu próximo jogo favorito com a ajuda da nossa IA</p>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-12">
            <div class="chat-container border rounded">
                <div class="chat-messages p-3">
                    <div class="message bot-message mb-2">
                        Olá! Sou seu assistente de jogos. Como posso te ajudar hoje? Você pode pedir por uma plataforma, categoria ou até mesmo descrever o tipo de jogo que você gosta.
                    </div>
                </div>
                <div class="chat-input-area p-3 border-top">
                    <div class="input-group">
                        <input type="text" id="chat-input" class="form-control" placeholder="Digite sua mensagem...">
                        <button class="btn btn-primary" type="button" id="send-chat-button">
                            <i class="bi bi-send-fill"></i> Enviar
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section Styles {
    <style>
        .chat-container {
            height: 70vh;
            display: flex;
            flex-direction: column;
            background-color: #f8f9fa;
        }

        .chat-messages {
            flex-grow: 1;
            overflow-y: auto;
        }

        .message {
            padding: 10px 15px;
            border-radius: 20px;
            margin-bottom: 10px;
            max-width: 80%;
            word-wrap: break-word;
        }

        .bot-message {
            background-color: #e9ecef;
            align-self: flex-start;
            text-align: left;
        }

        .user-message {
            background-color: #0d6efd;
            color: white;
            align-self: flex-end;
            margin-left: auto;
            text-align: right;
        }

        .row.flex-nowrap::-webkit-scrollbar {
            display: none;
        }
    </style>
}
@section Scripts {
    <script src="~/js/recomendacao.js" asp-append-version="true"></script>
}